/** Fewer distinct visitors than this is too few to name a source on its own row. */
export const MIN_SOURCE_VISITORS = 3;

/** More named rows than this is more than anyone reads; the rest fold into "Other". Also caps what a script can add. */
export const MAX_NAMED_ROWS = 12;
